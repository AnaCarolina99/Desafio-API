BANCO DE DADOS

O BD foi construído no SQL SERVER utilizando queries do banco, definindo as PK e FK de acordo com o exemplo passado, no diretório BD SQL SERVER se encontra os anexos

API - BACKEND

A API foi construída utilizando DDD e Dapper, primeiro passo foi aplicar as estruturas, segundo foi a criação das entidades, repositórios e interfaces 
e no terceiro passo foi construído os endpoints dentro do Swagger para o controle do CRUD da API
